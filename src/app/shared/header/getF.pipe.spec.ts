/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GetFPipe } from './getF.pipe';

describe('Pipe: GetFe', () => {
  it('create an instance', () => {
    let pipe = new GetFPipe();
    expect(pipe).toBeTruthy();
  });
});
